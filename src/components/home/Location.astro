---
const {location, address, number, } = Astro.props;
---

<div class="flex flex-col justify-center items-center">
	<p class="uppercase text-2xl font-extrabold text-black/80">{location}</p>
	<p class="text-lg text-center text-gray-600 leading-8 flex-1 px-4">{ address }</p>
	<a href={`tel:${number}`} class="hover:underline mt-2">
		<p class="font-extrabold text-dimGray text-lg">{ number }</p>
	</a>
</div>